commit 3904f93fa2946f0cb847a77b5314b91f03e391b1
Author: Mike Kolitz <mikekol@outlook.com>
Date:   Wed Jul 8 14:08:56 2026 -0700

    feat: broadcast messages to all configured remotes

diff --git a/TarkovMonitor/SocketClient.cs b/TarkovMonitor/SocketClient.cs
index 438a64d..293d14c 100644
--- a/TarkovMonitor/SocketClient.cs
+++ b/TarkovMonitor/SocketClient.cs
@@ -107,17 +107,29 @@ namespace TarkovMonitor
 
         public static async Task Send(List<JsonObject> messages)
         {
-            var remoteid = Properties.Settings.Default.remoteId;
-            if (remoteid == null || remoteid == "")
+            var remoteIdString = Properties.Settings.Default.remoteId;
+            if (remoteIdString == null || remoteIdString == "")
             {
                 return;
             }
+
             await VerifyClient();
+
+            var remotes = GetAllRemotes();
+            if (remotes.Count == 0)
+            {
+                return;
+            }
+
             foreach (var message in messages)
             {
-                message["sessionID"] = remoteid;
-                await socket.SendInstant(message.ToJsonString());
+                foreach (var remote in remotes)
+                {
+                    message["sessionID"] = remote.Id;
+                    await socket.SendInstant(message.ToJsonString());
+                }
             }
+
             idleTimer.Stop();
             idleTimer.Start();
         }

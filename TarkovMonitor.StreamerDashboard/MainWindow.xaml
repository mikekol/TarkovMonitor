<Window
    x:Class="TarkovMonitor.StreamerDashboard.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Tarkov Streamer Dashboard">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <NavigationView
        x:Name="NavView"
        Grid.Row="0"
        IsSettingsVisible="False"
        PaneDisplayMode="Left"
        OpenPaneLength="180"
        SelectionChanged="NavView_SelectionChanged">

      <NavigationView.MenuItems>
        <NavigationViewItem Content="Dashboard" Tag="Dashboard">
          <NavigationViewItem.Icon><SymbolIcon Symbol="Home"/></NavigationViewItem.Icon>
        </NavigationViewItem>
        <NavigationViewItem Content="Event Actions" Tag="EventActions">
          <NavigationViewItem.Icon><SymbolIcon Symbol="Setting"/></NavigationViewItem.Icon>
        </NavigationViewItem>
      </NavigationView.MenuItems>

      <Frame x:Name="ContentFrame"/>
    </NavigationView>

    <!-- Status bar -->
    <Border
        Grid.Row="1"
        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
        BorderThickness="0,1,0,0"
        Padding="16,5">
      <StackPanel Orientation="Horizontal" Spacing="20">
        <StackPanel Orientation="Horizontal" Spacing="7" VerticalAlignment="Center">
          <Ellipse x:Name="StatusDot" Width="9" Height="9" Fill="{ThemeResource SystemFillColorCautionBrush}"/>
          <TextBlock x:Name="StatusText" Text="Connecting…" FontSize="12" VerticalAlignment="Center"/>
        </StackPanel>
        <TextBlock
            x:Name="OverlayUrlText"
            FontSize="12"
            FontFamily="Consolas"
            VerticalAlignment="Center"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
      </StackPanel>
    </Border>
  </Grid>
</Window>
